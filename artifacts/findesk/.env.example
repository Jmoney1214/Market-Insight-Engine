# Supabase (browser plane). Both values are public by design — the publishable
# key ships in the bundle and RLS restricts it to read-only SELECTs. Defaults
# for the findesk project are baked into src/lib/supabase.ts; set these only to
# point at a different project.
VITE_SUPABASE_URL=https://ganihlwaijdxpigssyab.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_e3CMkui4gtv668USv2usSw__Mv8RqZz

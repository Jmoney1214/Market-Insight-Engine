{
  "name": "@workspace/mcp-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP gateway exposing the Market-Insight desk API as agent tools",
  "bin": {
    "mie-mcp-gateway": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vitest": "^4.1.9"
  }
}
